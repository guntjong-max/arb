require('dotenv').config(); const { createServer } = require('./server'); const logger = require('./config/logger'); async function start() { try { logger.info('Starting Arbitrage Bot Engine (MINIMAL MODE)...'); const app = await createServer(); const PORT = process.env.PORT || 3000; app.listen(PORT, () => { logger.info(`Engine HTTP Server listening on port ${PORT}`); logger.info(`Health check: http://localhost:${PORT}/health`); logger.info(`Sessions API: http://localhost:${PORT}/api/v1/sessions`); }); } catch (error) { logger.error('Failed to start engine:', error); process.exit(1); } } start();
