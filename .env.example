# Arbitrage Bot System - Environment Configuration

# Node Environment
NODE_ENV=development
PORT=3000
WS_PORT=3001

# Database Configuration
DATABASE_URL=postgresql://arbitrage_user:CHANGE_ME@postgres:5432/arbitrage_bot
DB_PASSWORD=CHANGE_ME_strong_password_123
POSTGRES_DB=arbitrage_bot
POSTGRES_USER=arbitrage_user

# Redis Configuration
REDIS_URL=redis://:CHANGE_ME@redis:6379
REDIS_PASSWORD=CHANGE_ME_redis_password_123

# Security Secrets
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=CHANGE_ME_generate_random_hex_here
SESSION_SECRET=CHANGE_ME_generate_random_hex_here

# Admin Credentials
PGADMIN_EMAIL=admin@arbitrage.local
PGADMIN_PASSWORD=CHANGE_ME_pgadmin_password
GRAFANA_USER=admin
GRAFANA_PASSWORD=CHANGE_ME_grafana_password

# Trading Configuration
PAPER_TRADING_MODE=true
ENABLE_DEBUG_LOGS=true

# Domain (for production)
DOMAIN=localhost

# Worker Configuration (for Python workers)
WORKER_ID=worker-1
ENGINE_URL=http://engine:3000
ENGINE_WS_URL=ws://engine:3000/ws
PROXY_SERVER=
PROXY_USERNAME=
PROXY_PASSWORD=
